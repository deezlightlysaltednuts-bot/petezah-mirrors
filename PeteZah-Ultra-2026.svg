<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xhtml="http://www.w3.org/1999/xhtml" width="100%" height="100%" viewBox="0 0 1920 1080">
  <foreignObject id="fo" width="1920" height="1080" x="0" y="0">
    <xhtml:html xmlns="http://www.w3.org/1999/xhtml" style="width:100%;height:100%;margin:0;padding:0;">
    <xhtml:head>
      <xhtml:title>PeteZah Study Suite</xhtml:title>
      <xhtml:style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html, body { width: 100%; height: 100%; overflow: hidden; background: #030712; }
        iframe { position: fixed; top: 0; left: 0; width: 100%; height: 100%; border: 0; background: #030712; }
      </xhtml:style>
    </xhtml:head>
    <xhtml:body>
      <xhtml:iframe id="fm" src="about:blank" allow="fullscreen; autoplay; encrypted-media; clipboard-write"></xhtml:iframe>
      <xhtml:script>
      //<![CDATA[
        (function() {
          function size() {
            var w = window.innerWidth || 1920, h = window.innerHeight || 1080;
            var svg = document.querySelector('svg');
            var fo = document.querySelector('foreignObject');
            if (svg) { svg.setAttribute('width', w); svg.setAttribute('height', h); svg.setAttribute('viewBox', '0 0 ' + w + ' ' + h); }
            if (fo) { fo.setAttribute('width', w); fo.setAttribute('height', h); }
          }
          size();
          window.addEventListener('resize', size);

          (async () => {
            try {
              var baseCdn = 'https://fastly.jsdelivr.net/gh/deezlightlysaltednuts-bot/petezah-mirrors@main/';
              var res = await fetch(baseCdn + 'index.html?t=' + Date.now()).catch(function() {
                return fetch('https://cdn.jsdelivr.net/gh/deezlightlysaltednuts-bot/petezah-mirrors@main/index.html');
              });
              var html = await res.text();
              html = html.replace(/(src|href)=["']\/?assets\//g, '$1="' + baseCdn + 'assets/');

              var frame = document.getElementById('fm');
              var fdoc = frame.contentDocument || frame.contentWindow.document;
              fdoc.open();
              fdoc.write(html);
              fdoc.close();
            } catch (err) {
              console.error('PeteZah Edge Loader Error:', err);
            }
          })();
        })();
      //]]>
      </xhtml:script>
    </xhtml:body>
    </xhtml:html>
  </foreignObject>
</svg>